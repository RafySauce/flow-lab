# Backlog — Flow Starters (INBOX)

Incoming work for the flow-foundry. Two kinds land here:

- **`fp-<slug>.md`** — flow-primer-briefs (clean intent, `truth-level: to-review`), per `../templates/flow-primer-brief-template.md`.
- **Foreign starters** — external workflow material, ingested untouched at `truth-level: claimed`, vetted at triage before anything is built.

Starters leave this folder one of two ways: scaffolded and (after the three gates) human-promoted to `../../icp-flows/`, or dropped with a decision-log entry. Only sanitized, `data-class: public` starters belong in this public repo — real work starters go in the internal instance.
