# Completed Flowspaces (DONE)

Flowspace designs that passed all three validation gates and were promoted to `truth-level: verified` **by the operator** — the foundry never places anything here itself.

In this public repo, only sanitized, generic flowspace *designs* (`data-class: public`) live here. Instantiated flowspaces carrying real work content live in employer tenancy per `../../methodology/mirroring-protocol.md`.
